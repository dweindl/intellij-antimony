# IntelliJ Platform Artifacts Repositories -> https://plugins.jetbrains.com/docs/intellij/intellij-artifacts.html

pluginGroup = com.github.dweindl.intellijAntimony
pluginName = antimony-language
pluginRepositoryUrl = https://github.com/dweindl/intellij-antimony
# SemVer format -> https://semver.org
pluginVersion = 0.0.2

# Supported build number ranges and IntelliJ Platform versions -> https://plugins.jetbrains.com/docs/intellij/build-number-ranges.html
pluginSinceBuild = 251
# Leave blank for no upper bound (stay compatible with future IDE releases); set e.g. "253.*" to cap it again.
pluginUntilBuild =
# Upper bound used only for the verifyPlugin IDE matrix (see pluginVerification.ides.select in
# build.gradle.kts). Kept separate from pluginUntilBuild: with pluginUntilBuild left blank, the verifier's
# default IDE-selection query becomes unbounded too and can pick not-yet-resolvable IDE builds (observed:
# it picked "2025.3", which isn't a published artifact). Use a bare branch number, not a "NNN.*" wildcard —
# the wildcard form fed into this filter is what picked the bad "2025.3" release. Bump periodically to
# widen the tested range.
pluginVerifierUntilBuild = 253

# IntelliJ Platform Properties -> https://plugins.jetbrains.com/docs/intellij/tools-gradle-intellij-plugin.html#configuration-intellij-extension
platformType = IC
platformVersion = 2025.1.1

# Plugin Dependencies -> https://plugins.jetbrains.com/docs/intellij/plugin-dependencies.html
# Example: platformPlugins = com.intellij.java, com.jetbrains.php:203.4449.22
platformPlugins = PythonCore:251.25410.129
# Example: platformBundledPlugins = com.intellij.java
platformBundledPlugins =

# Gradle Releases -> https://github.com/gradle/gradle/releases
gradleVersion = 9.6.1

# Opt-out flag for bundling Kotlin standard library -> https://jb.gg/intellij-platform-kotlin-stdlib
kotlin.stdlib.default.dependency = false

# Enable Gradle Configuration Cache -> https://docs.gradle.org/current/userguide/configuration_cache.html
org.gradle.configuration-cache = true

# Enable Gradle Build Cache -> https://docs.gradle.org/current/userguide/build_cache.html
org.gradle.caching = true
